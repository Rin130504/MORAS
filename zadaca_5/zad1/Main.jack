Class Main {
	function void main() {
        var Array A;
        var int size;
        
        let size = 5;
        let A = Array.new(size);
        let A[0] = 3;
        let A[1] = 1;
        let A[2] = 4;
        let A[3] = 1;
        let A[4] = 5;
        
        do qsort.sort(A, 0, size - 1);
        do qsort.printArray(A, size);
        
        return;
    }

}